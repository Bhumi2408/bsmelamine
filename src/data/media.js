// Centralised media paths that aren't tied to a specific product/category.
// Replace `introVideo` with the real file whenever it's ready — no
// component code needs to change.
export const media = {
  introVideo: "/video.mp4",
};
